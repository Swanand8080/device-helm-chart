apiVersion: v1
kind: Service
metadata:
  name: {{ printf "%s-device" .Release.Name }}
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.device.port | default 80 }}
    targetPort: {{ .Values.device.targetPort | default 80 }}
    protocol: TCP
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}